manifestVersion: 1
id: saltedlolly-npm-plus
category: networking
name: NPMplus
version: "2026-07-24-r1.01"
tagline: Publish your services with a modern, security-focused reverse proxy
description: >-
  ⚠️ UNDER CONSTRUCTION: this app is still being tested and may not install or work correctly yet. Check back soon.


  NPMplus makes it easy to publish services through a friendly web interface, with automatic TLS certificates, HTTP/3 support, access lists, stream forwarding, and advanced Nginx configuration.


  NPMplus has a built-in CrowdSec bouncer for community-sourced intrusion protection - point it at a CrowdSec instance once you have one running (a standalone CrowdSec app is not included and is planned separately).


  ⚠️ Publishing a service makes it reachable from the internet. Protect every exposed service with appropriate authentication and keep both NPMplus and the upstream service updated.


  Open NPMplus from the Umbrel home screen for browser-based setup instructions and a button to launch its HTTPS administration interface. Your initial username is admin@umbrel.local and Umbrel displays the unique initial password in the app's credentials.


  Router port forwarding:


  - Forward external TCP port 80 to port 50080 on your Umbrel.

  - Forward external TCP port 443 to port 50443 on your Umbrel.

  - Forward external UDP port 443 to port 50443 on your Umbrel for HTTP/3.


  All NPMplus configuration, certificates, users, and proxy hosts are stored under this app's persistent data directory and survive app restarts and updates.


  ⚠️ Uninstalling this app deletes its persistent app data. Back it up before uninstalling or migrating an existing installation.
releaseNotes: >-
  Fix launcher image packaging issue - Use pre-built launcher image from GHCR instead of local build


  This release fixes the "unable to prepare context" error that occurred when updating to the previous version. The launcher is now built and pushed to GitHub Container Registry as a proper multi-architecture image.


  Previous release features (Trusted Proxy Configuration UI):

  - Web-based configuration for Cloudflare proxy mode (TRUST_CLOUDFLARE setting)

  - No terminal access required to configure trusted proxy settings

  - Configuration persists across app restarts and updates

  - Simplified launcher page focused on setup workflow


  How to use:

  1. Open the NPMplus app (port 5180)

  2. Configure "Trusted Proxy Configuration" if using Cloudflare's proxy (orange cloud)

  3. Click "Save Settings"

  4. Restart the app via Umbrel dashboard


  This package pins the immutable multi-architecture NPMplus 2026-07-24-r1 image for amd64 and arm64.


  Full upstream release notes: https://github.com/ZoeyVid/NPMplus/releases/tag/2026-07-24-r1

developer: ZoeyVid
website: https://github.com/ZoeyVid/NPMplus
dependencies: []
repo: https://github.com/ZoeyVid/NPMplus
support: https://github.com/ZoeyVid/NPMplus/discussions

port: 5180
gallery: []
path: ""

defaultUsername: admin@umbrel.local
defaultPassword: ""
deterministicPassword: true

icon: https://raw.githubusercontent.com/saltedlolly/umbrel-app-store-gallery/master/saltedlolly-npm-plus/npmplusicon.svg
submitter: saltedlolly
submission: https://github.com/saltedlolly/umbrel-app-store/tree/master/saltedlolly-npm-plus
